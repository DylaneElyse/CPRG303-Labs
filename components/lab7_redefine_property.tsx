import { TextInput, StyleSheet, View, Button, Text } from "react-native";

export default function RedefineProperty(param: any) {
return(
    <TextInput placeholder={"Placeholder"}/>
);
}







const styles = StyleSheet.create({
    container:{
        
    }
})